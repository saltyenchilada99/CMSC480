/**
 * Jest DOM test setup.
 *
 * The import adds DOM-specific matchers such as toHaveTextContent and
 * toBeInTheDocument to every React Testing Library test.
 */
import '@testing-library/jest-dom';
